# Stub — swap diarize.py re-export to activate
# Required env vars: GOOGLE_APPLICATION_CREDENTIALS, GCP_PROJECT_ID

def diarize(_audio_path: str) -> list[dict]:
    raise NotImplementedError(
        'Google Speech diarization not yet implemented. '
        'Set GOOGLE_APPLICATION_CREDENTIALS in .env and build this out.'
    )
