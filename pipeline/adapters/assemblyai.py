# Stub — swap diarize.py re-export to activate
# Required env vars: ASSEMBLYAI_API_KEY
# HIPAA-eligible with signed BAA

def diarize(_audio_path: str) -> list[dict]:
    raise NotImplementedError(
        'AssemblyAI diarization not yet implemented. '
        'Set ASSEMBLYAI_API_KEY in .env and build this out.'
    )
