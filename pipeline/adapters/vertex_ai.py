# Stub — swap llm.py re-export to activate
# Required env vars: GOOGLE_APPLICATION_CREDENTIALS, GCP_PROJECT_ID

def map_segments(_segments: list[dict]) -> list[dict]:
    raise NotImplementedError(
        'Vertex AI adapter not yet implemented. '
        'Set GOOGLE_APPLICATION_CREDENTIALS and GCP_PROJECT_ID in .env.'
    )
