# Stub — swap llm.py re-export to activate
# Required env vars: AZURE_OPENAI_ENDPOINT, AZURE_OPENAI_KEY, AZURE_OPENAI_DEPLOYMENT

def map_segments(_segments: list[dict]) -> list[dict]:
    raise NotImplementedError(
        'Azure OpenAI adapter not yet implemented. '
        'Set AZURE_OPENAI_ENDPOINT, AZURE_OPENAI_KEY, and AZURE_OPENAI_DEPLOYMENT in .env.'
    )
